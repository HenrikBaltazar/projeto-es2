Se quiser testar:
- Precisa ter o PHP instalado no PC
- O BD também rodando com a configuração de

  admin: root // senha: root // porta: 3306
  
	Schema twitter_clone
    ->tabelas
    
    create table usuarios(
      id int not null primary key AUTO_INCREMENT,
      nome varchar(100) not null,
      email varchar(150),
      senha varchar(32) not null
	);

    create table tweets(
    	id int not null PRIMARY KEY AUTO_INCREMENT,
    	id_usuario int not null,
    	tweet varchar(140) not null,
    	data datetime default current_timestamp
    );

  create table usuarios_seguidores(
  	id int not null primary key auto_increment,
  	id_usuario int not null,
  	id_usuario_seguindo int not null
  );


- Descompacte a pasta
- Acesse a pasta public
- Na barra que mostra o caminho digite cmd
- Ao abrir o terminal, digite php -S localhost:8080
- Já está rodando a aplicação, só acessar localhost:8080 no navegador
